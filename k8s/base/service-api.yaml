apiVersion: v1
kind: Service
metadata:
  name: hexagonal-golang-api-service
spec:
  type: NodePort
  selector:
    app: hexagonal-golang-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080 